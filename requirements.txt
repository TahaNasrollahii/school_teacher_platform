# ============================================================
# School-Teacher Matching Platform – Python dependencies
# ============================================================
# Install all:          pip install -r requirements.txt
# Install runtime only: pip install -r requirements.txt --no-deps (see below)
# ============================================================

# ------------------------------------------------------------
# Runtime dependencies
# ------------------------------------------------------------
Django==6.0.6
djangorestframework==3.17.1
django-cors-headers==4.9.0
django-unfold==0.97.0

# ------------------------------------------------------------
# Development / test dependencies
# ------------------------------------------------------------
pytest==9.0.3
pytest-django==4.12.0
factory-boy==3.3.3
Faker==40.21.0

# Required for `python manage.py compilemessages` (i18n)
# Note: also needs the system package `gettext` (apt install gettext)
Babel==2.18.0
